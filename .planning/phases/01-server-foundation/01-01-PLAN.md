---
phase: 01-server-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/types.ts
  - package.json
  - vitest.config.ts
  - tests/shared/types.test.ts
autonomous: true
requirements:
  - TYPES-01
  - TYPES-02
  - TYPES-03
  - TYPES-04
  - TYPES-05

must_haves:
  truths:
    - "A TypeScript file can import TiptapPresetConfig and get compile-time errors for unknown feature keys"
    - "A TypeScript file can import TiptapPluginConfig and get compile-time validation for the presets Record shape"
    - "PRESET_FEATURE_KEYS contains exactly 17 entries covering all TiptapPresetConfig keys"
    - "isFeatureEnabled(undefined) returns true (absent key = feature enabled)"
    - "isFeatureEnabled(false) returns false and isFeatureEnabled(true) returns true"
    - "getFeatureOptions(false, defaults) returns null (explicitly disabled)"
    - "getFeatureOptions(true, defaults) returns the defaults object"
    - "getFeatureOptions({ levels: [2] }, { levels: [1,2,3] }) returns merged object"
    - "MINIMAL_PRESET_CONFIG equals { bold: true, italic: true }"
  artifacts:
    - path: "shared/types.ts"
      provides: "TiptapPresetConfig, TiptapPluginConfig, PRESET_FEATURE_KEYS, isFeatureEnabled, getFeatureOptions, MINIMAL_PRESET_CONFIG"
      exports:
        - TiptapPresetConfig
        - TiptapPluginConfig
        - PRESET_FEATURE_KEYS
        - isFeatureEnabled
        - getFeatureOptions
        - MINIMAL_PRESET_CONFIG
    - path: "vitest.config.ts"
      provides: "Vitest test framework configuration"
      contains: "tests/"
    - path: "tests/shared/types.test.ts"
      provides: "Unit tests for TYPES-03, TYPES-04, TYPES-05"
  key_links:
    - from: "server/src/"
      to: "shared/types.ts"
      via: "relative import '../../../shared/types'"
      pattern: "from '.*shared/types'"
    - from: "tests/shared/types.test.ts"
      to: "shared/types.ts"
      via: "import { isFeatureEnabled, getFeatureOptions, PRESET_FEATURE_KEYS }"
      pattern: "isFeatureEnabled|getFeatureOptions|PRESET_FEATURE_KEYS"
---

<objective>
Create the shared TypeScript type contracts and utility functions that all downstream server and admin code depends on. Install Vitest and write the test scaffold so every subsequent plan has automated verification from the first task.

Purpose: Shared types are the foundation — without compile-time contracts, downstream phases would have no type safety and the utilities (isFeatureEnabled, getFeatureOptions) would be untestable functions that diverge silently from the requirements.

Output: `shared/types.ts` with full type definitions and implementations, `vitest.config.ts`, `tests/shared/types.test.ts` with green unit tests, and `package.json` updated with Vitest devDependency and test script.
</objective>

<execution_context>
@/Users/mbp-13/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbp-13/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-server-foundation/01-RESEARCH.md

<interfaces>
<!-- Canonical type declarations — from dist/shared/types.d.ts (verified source of truth) -->
<!-- These are the DECLARATIONS. shared/types.ts must provide the IMPLEMENTATIONS. -->

From dist/shared/types.d.ts:
```typescript
export type HeadingLevel = 1 | 2 | 3 | 4 | 5 | 6;

export type LinkConfig = {
  openOnClick?: boolean;
  HTMLAttributes?: Record<string, string>;
};

export type TableConfig = {
  resizable?: boolean;
};

export type TextAlignConfig = {
  types?: string[];
  alignments?: Array<'left' | 'center' | 'right' | 'justify'>;
};

export type HeadingConfig = {
  levels?: HeadingLevel[];
};

export interface TiptapPresetConfig {
  bold?: boolean | Record<string, unknown>;
  italic?: boolean | Record<string, unknown>;
  strike?: boolean | Record<string, unknown>;
  code?: boolean | Record<string, unknown>;
  codeBlock?: boolean | Record<string, unknown>;
  blockquote?: boolean | Record<string, unknown>;
  bulletList?: boolean | Record<string, unknown>;
  orderedList?: boolean | Record<string, unknown>;
  hardBreak?: boolean | Record<string, unknown>;
  horizontalRule?: boolean | Record<string, unknown>;
  history?: boolean | Record<string, unknown>;
  heading?: boolean | HeadingConfig;
  link?: boolean | LinkConfig;
  table?: boolean | TableConfig;
  textAlign?: boolean | TextAlignConfig;
  superscript?: boolean | Record<string, unknown>;
  subscript?: boolean | Record<string, unknown>;
}

export interface TiptapPluginConfig {
  presets: Record<string, TiptapPresetConfig>;
}
```

From shared/pluginId.ts (existing sibling file — confirms shared/ is importable from server/src via '../../../shared/'):
```typescript
export const PLUGIN_ID = 'tiptap-editor';
```

From server/tsconfig.json:
```json
{ "compilerOptions": { "rootDir": "../" } }
```
rootDir is the repo root (parent of server/), so imports from shared/ are allowed from server/src/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and create test infrastructure</name>
  <files>package.json, vitest.config.ts, tests/shared/types.test.ts, tests/server/config.test.ts, tests/server/preset.service.test.ts, tests/server/preset.controller.test.ts, tests/server/routes.test.ts</files>
  <action>
Install Vitest as a devDependency and configure the test infrastructure before any source code exists.

1. Add `vitest` to devDependencies in `package.json`. Run: `yarn add --dev vitest`. Then add two scripts to the `scripts` block:
   - `"test": "vitest run"`
   - `"test:watch": "vitest"`

   Do NOT remove any existing scripts (test:ts:front, test:ts:back, etc.).

2. Create `vitest.config.ts` at the repo root with minimal config:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['tests/**/*.test.ts'],
    environment: 'node',
  },
});
```

3. Create `tests/shared/types.test.ts` as a FAILING test stub (RED phase) that documents all required behaviors for TYPES-03, TYPES-04, TYPES-05:
```typescript
import { describe, it, expect } from 'vitest';
import {
  PRESET_FEATURE_KEYS,
  MINIMAL_PRESET_CONFIG,
  isFeatureEnabled,
  getFeatureOptions,
} from '../../shared/types';

describe('PRESET_FEATURE_KEYS (TYPES-03)', () => {
  it('contains exactly 17 keys', () => {
    expect(PRESET_FEATURE_KEYS).toHaveLength(17);
  });
  it('contains all expected feature names', () => {
    expect(PRESET_FEATURE_KEYS).toContain('bold');
    expect(PRESET_FEATURE_KEYS).toContain('italic');
    expect(PRESET_FEATURE_KEYS).toContain('heading');
    expect(PRESET_FEATURE_KEYS).toContain('table');
    expect(PRESET_FEATURE_KEYS).toContain('textAlign');
    expect(PRESET_FEATURE_KEYS).toContain('superscript');
    expect(PRESET_FEATURE_KEYS).toContain('subscript');
  });
});

describe('MINIMAL_PRESET_CONFIG', () => {
  it('equals { bold: true, italic: true }', () => {
    expect(MINIMAL_PRESET_CONFIG).toStrictEqual({ bold: true, italic: true });
  });
});

describe('isFeatureEnabled (TYPES-04)', () => {
  it('returns true for undefined (absent key = enabled by default)', () => {
    expect(isFeatureEnabled(undefined)).toBe(true);
  });
  it('returns true for true', () => {
    expect(isFeatureEnabled(true)).toBe(true);
  });
  it('returns false for false', () => {
    expect(isFeatureEnabled(false)).toBe(false);
  });
  it('returns true for an options object (feature enabled with config)', () => {
    expect(isFeatureEnabled({ levels: [1, 2] })).toBe(true);
  });
  it('returns false for an object with enabled: false', () => {
    expect(isFeatureEnabled({ enabled: false })).toBe(false);
  });
});

describe('getFeatureOptions (TYPES-05)', () => {
  it('returns null when value is false (explicitly disabled)', () => {
    expect(getFeatureOptions(false, { x: 1 })).toBeNull();
  });
  it('returns defaults when value is true', () => {
    expect(getFeatureOptions(true, { x: 1 })).toStrictEqual({ x: 1 });
  });
  it('returns defaults when value is undefined', () => {
    expect(getFeatureOptions(undefined, { x: 1 })).toStrictEqual({ x: 1 });
  });
  it('merges options object with defaults (options override defaults)', () => {
    expect(getFeatureOptions({ a: 2 }, { a: 1, b: 3 })).toStrictEqual({ a: 2, b: 3 });
  });
});
```

4. Create stub test files for server tests (they will be populated in Plan 02):

`tests/server/config.test.ts`:
```typescript
// Tests for SERVER-01: config validator
// Populated in Plan 02 after server/src/config/index.ts is implemented
import { describe, it } from 'vitest';

describe('config validator (SERVER-01)', () => {
  it.todo('throws for preset with invalid feature key, naming the bad key');
  it.todo('passes for valid preset { bold: true, heading: { levels: [1, 2] } }');
});
```

`tests/server/preset.service.test.ts`:
```typescript
// Tests for SERVER-02, SERVER-03: preset service
// Populated in Plan 02
import { describe, it } from 'vitest';

describe('preset service (SERVER-02, SERVER-03)', () => {
  it.todo('listPresetNames() returns correct keys from mock config');
  it.todo('getPreset() returns config for known name');
  it.todo('getPreset() returns null for unknown name');
});
```

`tests/server/preset.controller.test.ts`:
```typescript
// Tests for SERVER-03: controller fallback behavior
// Populated in Plan 02
import { describe, it } from 'vitest';

describe('preset controller (SERVER-03)', () => {
  it.todo('findOne sets ctx.body to MINIMAL_PRESET_CONFIG for unknown preset name');
});
```

`tests/server/routes.test.ts`:
```typescript
// Tests for SERVER-04: routes use type: 'admin'
// Populated in Plan 02
import { describe, it } from 'vitest';

describe('routes (SERVER-04)', () => {
  it.todo('routes export contains type: admin');
  it.todo('route paths are /presets and /presets/:name');
});
```
  </action>
  <verify>
    <automated>cd /Users/mbp-13/_work/notum-plugins/strapi-plugin-tiptap-editor && yarn test 2>&1 | tail -20</automated>
  </verify>
  <done>
    `yarn test` exits non-zero (tests fail with import error because shared/types.ts doesn't exist yet — this is expected RED state). All 5 test files exist. `package.json` has `vitest` in devDependencies and `"test": "vitest run"` in scripts. `vitest.config.ts` exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared/types.ts (GREEN phase)</name>
  <files>shared/types.ts</files>
  <behavior>
    - PRESET_FEATURE_KEYS has length 17
    - PRESET_FEATURE_KEYS contains 'bold', 'italic', 'strike', 'code', 'codeBlock', 'blockquote', 'bulletList', 'orderedList', 'hardBreak', 'horizontalRule', 'history', 'heading', 'link', 'table', 'textAlign', 'superscript', 'subscript'
    - MINIMAL_PRESET_CONFIG deep-equals { bold: true, italic: true }
    - isFeatureEnabled(undefined) === true (DIVERGES FROM DIST — absent key means enabled)
    - isFeatureEnabled(true) === true
    - isFeatureEnabled(false) === false
    - isFeatureEnabled({ levels: [1,2] }) === true (options object = enabled)
    - isFeatureEnabled({ enabled: false }) === false
    - getFeatureOptions(false, { x: 1 }) === null (explicitly disabled — DIVERGES FROM DIST which returns defaults)
    - getFeatureOptions(true, { x: 1 }) deep-equals { x: 1 }
    - getFeatureOptions(undefined, { x: 1 }) deep-equals { x: 1 }
    - getFeatureOptions({ a: 2 }, { a: 1, b: 3 }) deep-equals { a: 2, b: 3 }
  </behavior>
  <action>
Create `shared/types.ts` with all exported types, constants, and utility functions. The tests from Task 1 are already written — implement until they pass.

CRITICAL DIVERGENCES FROM DIST (requirements-mandated):
- `isFeatureEnabled(undefined)` must return `true`, NOT `false` (dist returns false)
- `getFeatureOptions(false, defaults)` must return `null`, NOT `defaults` (dist returns defaults)

```typescript
// shared/types.ts

// ─── Option types ───────────────────────────────────────────────────────────

export type HeadingLevel = 1 | 2 | 3 | 4 | 5 | 6;

export type LinkConfig = {
  openOnClick?: boolean;
  HTMLAttributes?: Record<string, string>;
};

export type TableConfig = {
  resizable?: boolean;
};

export type TextAlignConfig = {
  types?: string[];
  alignments?: Array<'left' | 'center' | 'right' | 'justify'>;
};

export type HeadingConfig = {
  levels?: HeadingLevel[];
};

// ─── Core types ──────────────────────────────────────────────────────────────

export interface TiptapPresetConfig {
  bold?: boolean | Record<string, unknown>;
  italic?: boolean | Record<string, unknown>;
  strike?: boolean | Record<string, unknown>;
  code?: boolean | Record<string, unknown>;
  codeBlock?: boolean | Record<string, unknown>;
  blockquote?: boolean | Record<string, unknown>;
  bulletList?: boolean | Record<string, unknown>;
  orderedList?: boolean | Record<string, unknown>;
  hardBreak?: boolean | Record<string, unknown>;
  horizontalRule?: boolean | Record<string, unknown>;
  history?: boolean | Record<string, unknown>;
  heading?: boolean | HeadingConfig;
  link?: boolean | LinkConfig;
  table?: boolean | TableConfig;
  textAlign?: boolean | TextAlignConfig;
  superscript?: boolean | Record<string, unknown>;
  subscript?: boolean | Record<string, unknown>;
}

export interface TiptapPluginConfig {
  presets: Record<string, TiptapPresetConfig>;
}

// ─── Constants ───────────────────────────────────────────────────────────────

export const PRESET_FEATURE_KEYS: Array<keyof TiptapPresetConfig> = [
  'bold', 'italic', 'strike', 'code', 'codeBlock', 'blockquote',
  'bulletList', 'orderedList', 'hardBreak', 'horizontalRule', 'history',
  'heading', 'link', 'table', 'textAlign', 'superscript', 'subscript',
];

// Fallback for unconfigured fields — deliberately minimal to prompt developers to configure
export const MINIMAL_PRESET_CONFIG: TiptapPresetConfig = {
  bold: true,
  italic: true,
};

// ─── Internal helpers ────────────────────────────────────────────────────────

const isPlainObject = (value: unknown): value is Record<string, unknown> => {
  if (!value || typeof value !== 'object' || Array.isArray(value)) return false;
  const prototype = Object.getPrototypeOf(value);
  return prototype === Object.prototype || prototype === null;
};

// ─── Utility functions ───────────────────────────────────────────────────────

/**
 * Returns true when a preset feature value is enabled.
 * NOTE: undefined (absent key) returns true — absent key = feature enabled by default.
 * This DIVERGES from dist where undefined returns false.
 */
export const isFeatureEnabled = (
  value: TiptapPresetConfig[keyof TiptapPresetConfig]
): boolean => {
  if (value === undefined) {
    return true; // TYPES-04: absent key = enabled
  }
  if (typeof value === 'boolean') {
    return value;
  }
  if (!isPlainObject(value)) {
    return false;
  }
  const obj = value as Record<string, unknown>;
  if (typeof obj.enabled === 'boolean') {
    return obj.enabled;
  }
  if (typeof obj.disabled === 'boolean') {
    return !obj.disabled;
  }
  return true;
};

/**
 * Returns the effective options for a feature.
 * - false → null (feature explicitly disabled, no options)
 * - true | undefined → defaults (feature enabled with default options)
 * - object → merged with defaults (feature enabled with custom options)
 *
 * NOTE: false returns null — DIVERGES from dist where false returns defaults.
 */
export const getFeatureOptions = <T extends Record<string, unknown>>(
  value: boolean | T | undefined,
  defaults: T
): T | null => {
  if (value === false) {
    return null; // TYPES-05: explicitly disabled
  }
  if (!isPlainObject(value)) {
    return defaults; // true or undefined: return defaults
  }
  const { enabled: _e, disabled: _d, ...rest } = value as Record<string, unknown>;
  return { ...defaults, ...rest } as T;
};
```

After creating the file, run `yarn test:ts:back` to verify TypeScript compiles cleanly, then `yarn test` to verify all tests in `tests/shared/types.test.ts` pass.
  </action>
  <verify>
    <automated>cd /Users/mbp-13/_work/notum-plugins/strapi-plugin-tiptap-editor && yarn test:ts:back && yarn test</automated>
  </verify>
  <done>
    `yarn test:ts:back` exits 0 (no TypeScript errors). `yarn test` exits 0 with all tests in `tests/shared/types.test.ts` passing (16 assertions green). Stub test files have their `it.todo` entries shown as skipped, not failed.
  </done>
</task>

</tasks>

<verification>
Final verification for Plan 01:

1. `yarn test:ts:back` — TypeScript compiles without errors (validates TYPES-01 and TYPES-02 indirectly by ensuring server/src files can import shared types without errors)
2. `yarn test` — All tests in `tests/shared/types.test.ts` pass (validates TYPES-03, TYPES-04, TYPES-05)
3. `shared/types.ts` exports: `TiptapPresetConfig`, `TiptapPluginConfig`, `PRESET_FEATURE_KEYS`, `isFeatureEnabled`, `getFeatureOptions`, `MINIMAL_PRESET_CONFIG`
4. All server test stubs exist at `tests/server/*.test.ts`
</verification>

<success_criteria>
- `yarn test:ts:back` exits 0
- `yarn test` exits 0 with 16 passing assertions in types.test.ts and 4 stub files with todo items
- `shared/types.ts` exists and exports all 6 symbols
- `vitest.config.ts` exists at repo root
- `package.json` has `vitest` in devDependencies and `"test": "vitest run"` in scripts
</success_criteria>

<output>
After completion, create `.planning/phases/01-server-foundation/01-01-SUMMARY.md` following the summary template at `@/Users/mbp-13/.claude/get-shit-done/templates/summary.md`.
</output>
