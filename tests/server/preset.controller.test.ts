// Tests for SERVER-03: controller fallback behavior
// Populated in Plan 02
import { describe, it } from 'vitest';

describe('preset controller (SERVER-03)', () => {
  it.todo('findOne sets ctx.body to MINIMAL_PRESET_CONFIG for unknown preset name');
});
